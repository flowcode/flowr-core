{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'Setting'}, 'Setting') }} - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>{{ 'Setting' | trans({}, 'Setting')}}</h1>
        <ol class="breadcrumb">
            <li><a href={{ path('dashboard') }}><i class="fa fa-dashboard"></i> {{"Dashboard" | trans({}, 'Setting')}}</a></li>
            <li><a href="{{ path('setting') }}"> {{ '%entity% List'|trans({'%entity%': 'Setting'}, 'Setting') }}</a></li>
            <li class="active" >{{ 'New %entity%'|trans({'%entity%': 'Setting'}, 'Setting') }}</li>
        </ol>
    </section>        
    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{ '%entity% creation'|trans({'%entity%': 'Setting'}, 'Setting') }}</h3>
            </div>
            <div class="box-body">
                {{ form_start(form, {action: path('setting_create')}) }}
                    {{ form_widget(form) }}
                    <p>
                        <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'Setting') }}</button>
                    </p>
                {{ form_end(form) }}
            </div>
            <div class="box-footer">
                        
        <div class="btn-group record_actions">


        </div>

            </div>
        </div>
    </section>
{% endblock %}
